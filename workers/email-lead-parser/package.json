{
  "name": "fub-email-lead-parser",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Email Worker that parses inbound lead emails and posts to Follow Up Boss /v1/events",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "secret:fub": "wrangler secret put FUB_API_KEY",
    "test": "vitest run parsers.test.js"
  },
  "dependencies": {
    "postal-mime": "^2.4.3"
  },
  "devDependencies": {
    "vitest": "^4.0.18",
    "wrangler": "^3.78.12"
  }
}
